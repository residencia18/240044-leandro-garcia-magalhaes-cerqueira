import { CalcularIdadePipe } from './calcular-idade.pipe';

describe('CalcularIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
